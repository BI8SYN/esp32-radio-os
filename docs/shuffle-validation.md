# 随机换台开发验证（2026-09-23）

本次为 1.1.8 的开发内容，版本号仍为 1.1.7；没有签名发布、创建 tag 或修改 OTA 清单。

## 行为

- 顶部 Wi-Fi 左侧点击随机图标切换；灰色关闭，主题色及浅底表示开启。
- 默认关闭，NVS 保存选择；切换模式不打断当前播放。
- 下一台从当前播放来源抽取，排除当前流地址；上一台回退最近 12 次随机切台记录。
- 单一当前台不重连；空收藏沿用推荐回退；从列表直接选台会清空历史。
- 目录筛选在选台时固定，浏览其他筛选不改变播放池。筛选及历史不跨重启保存。
- 开启睡眠定时后，顶部时间和倒计时上下排列，支持 12/24 小时制。

## 自动验证

`tests/host/run.sh` 使用真实应用/偏好代码及 ASan、UBSan：
随机避免当前台、历史回退/容量上限、无历史不切台、单台不重连、空收藏回退、
筛选范围冻结、偏好读取，以及已有顺序切台、睡眠、联网与兼容性测试通过。

开发测试构建与普通构建均成功，无编译警告。普通构建 `RADIO_UI_TEST=OFF`，
镜像 2,740,160 字节，4 MiB 应用分区剩余 35%。SHA-256：

`393586571a64ea7db5cfae37573794d829b1c671cb01b21a63c4fcacbcc4eed0`

## 开发板验证

重新读取整片 16 MiB Flash，确认当前为独立 Radio OS 分区布局：`ota_0=0x20000`，
`ota_1=0x420000`，活动 OTA 序号 1、状态 valid。仅写应用区，最终恢复测试前 NVS。
本地回滚备份及截图保存在忽略目录 `backup/shuffle-20260923/`。

`tests/device/shuffle_ui.py` 专项通过：

- 真实 LVGL 输入路径点击开关，不触发播放；3 轮下一台/上一台验证选台与历史一致。
- 明暗主题重建后图标保留状态；12 小时制与 90 分钟倒计时画面没有重叠。
- 相邻 Wi-Fi、菜单按钮正常；息屏后点击随机所在位置只唤醒，不误切换。
- 重启后 `shuffle=1` 恢复；真实网络播放进入 playing；设备自检通过。

首轮测试曾使用音频线程台名断言，切流期间旧台名的短暂保留导致误判；专项改为读取
UI 模型流地址的哈希（不输出流地址），单独检查音频实际进入 playing，之后完整通过。
测试构建专项结束自检：内部 RAM 67,303、最大块 31,744、最低值 31,551 字节。

画面来自开发板 LVGL 渲染，触摸使用开发测试输入注入；未做真人手指精度、主观听音
和多小时稳定性测试。普通交付构建不包含输入注入或截图测试接口。

最终普通构建已烧录，应用与 NVS 均通过写后哈希校验；重启后 Wi-Fi 已连接，
设置页/播放页导航正常，`uitest state` 返回 unknown。最终 `SELFTEST PASS`：
内部 RAM 75,919、最大块 31,744、最低值 55,367、PSRAM 5,532,192 字节。
恢复原有设置，随机默认关闭，可由顶部按钮开启。
