# 1.1.8 正式发布验证

发布时间：2026-09-24（Asia/Shanghai）。代码及版本 tag：`5c411db` / `v1.1.8`。

- main CI `35885560282`：应用逻辑、设置兼容性及固件构建通过。
- Release 流水线 `35885561237`：通过，生成两份镜像及 SHA256SUMS。
- 以 GitHub Release 产物作为正式发布镜像，OTA 与 GitHub 应用镜像逐字节一致。
- 应用内嵌工程名 `radio`、版本 `1.1.8`，镜像校验通过。
- 正式包已烧录开发板 `ota_0=0x20000`，保留 NVS 设置；写后摘要校验通过。
- 正式包真机 `SELFTEST PASS`：内部 RAM 75,967、最大块 31,744、最低值 55,291、PSRAM 5,532,192 字节；测试命令不可用，设置页/播放页导航正常。
- 签名由发布服务器内的私钥完成，使用固件内置公钥验证通过。
- OTA 清单原子替换，旧清单保留为 `manifest.json.rollback-1.1.7-20260923T160552Z`。
- 公网重新下载清单与固件，ECDSA 签名、SHA-256 和 GitHub 镜像一致性全部通过。

正式产物 SHA-256：

```text
1b02490c94bbf82c6cd51559b22d67ae199f0bcd0e5dda5b63c7ee080e13ea93  radio-os-1.1.8-ota.bin
2026e99639e4b5f07ca8f98a62bf61fb10f4bcc0ebeeb3f4d65d84bbdbde2b2b  radio-os-1.1.8-0x0.bin
```

本地预检包和此前开发包只用于发布门禁，不作为公开 1.1.8 产物。主分支上的签名清单快照在正式 CI 产物验证、部署完成后归档，不移动或覆盖发布 tag。
